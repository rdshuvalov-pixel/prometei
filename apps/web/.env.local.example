# Публичный URL проекта Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co

# Сервисная роль — только на сервере (Vercel Environment Variables), не в браузер
SUPABASE_SERVICE_ROLE_KEY=

# Опционально: если задано, POST /api/jobs требует Authorization: Bearer <значение>
# ENQUEUE_SECRET=
